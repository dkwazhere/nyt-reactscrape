import React from "react";

const Navbar = () => (
  <div>
    <div class="jumbotron jumbotron-fluid">
      <div class="container">
        <h1 class="display-4">New York Times Scraper</h1>
        <p class="lead">Search for an article, Save an article, Comment on the article!</p>
      </div>
    </div>
  </div>
);

export default Navbar;
